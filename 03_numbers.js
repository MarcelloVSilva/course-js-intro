let value = 42;
let valueModified;

// transforma number in string
valueModified = value.toString();
console.log(typeof value, typeof valueModified);

// divisão
valueModified = value / 10;

// resto
valueModified = value % 10;

// adição
valueModified = value + 10;

// subração
valueModified = value - 10;

console.log(valueModified);
